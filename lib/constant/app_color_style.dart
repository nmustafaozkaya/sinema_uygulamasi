import 'package:flutter/material.dart';

class AppColorStyle {
  // Genel arka plan rengi
  static const Color scaffoldBackground = Color(0xFF0D1B2A);

  // AppBar rengi
  static const Color appBarColor = Color(0xFF1B263B);

  // Temel vurgu rengi (örneğin: butonlar, ikonlar)
  static const Color primaryAccent = Color(0xFF415A77);

  // İkincil vurgu rengi (hover veya seçilen elemanlar)
  static const Color secondaryAccent = Color(0xFF778DA9);

  // Yazılar için açık renk
  static const Color textPrimary = Color(0xFFE0E1DD);

  // İkincil yazılar (yardımcı metinler, açıklamalar)
  static const Color textSecondary = Color(0xFFA8B5C3);

  static const Color errorColor = Colors.red;
}
